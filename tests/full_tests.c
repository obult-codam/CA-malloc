#include "otest.h"
#include <ft_malloc.h>
#include <stdio.h>

int main(void)
{
    // void *ptr;

    // ptr = malloc(64);
    // printf("inbetween\n");
    // if (ptr == NULL) {
    //     printf("owowow\n");
    // }
    // show_alloc_mem();
    // free(ptr);
    // printf("free was run\n");
    // // show_alloc_mem();
    return 0;
}