void	*create_alloc_strategy(t_zone_header *zone, size_t size)
{
	// set local allor header to array header
	// check if there is a free alloc witht eh flag array
	// return ptr to the one in use or NULL
}
